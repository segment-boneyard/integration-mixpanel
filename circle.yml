machine:
  environment:
    WORKER_VERSION: 3.x
  node:
    version: 6.9.2
  python:
    version: 2.7.3
  services:
    - redis
    - docker

dependencies:
  pre:
   - pip install awscli

deployment:
  push:
   branch: master
   commands:
    - curl -sLu "$HTTP_AUTH:" $BUILD | sh

